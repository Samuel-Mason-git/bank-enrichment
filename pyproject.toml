[project]
name = "bank-enrichment"
version = "0.1.0"
description = "Enriching bank information with user context"
authors = [
    {name = "Samuel Mason"}
]
license = "MIT"
requires-python = ">=3.14"
dependencies = [
    "python-dotenv (>=1.2.2,<2.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
